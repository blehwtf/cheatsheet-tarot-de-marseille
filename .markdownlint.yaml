default: true

# Allow long lines in markdown (tables, links)
MD013:
  line_length: 120
  tables: false
  code_blocks: false

# Allow duplicate headings in different sections
MD024:
  siblings_only: true

# Allow raw HTML (nanoc content may include it)
MD033: false
